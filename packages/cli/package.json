{
  "name": "sheetmerge-cli",
  "private": true,
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "sheetmerge": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:e2e": "vitest run -c vitest.e2e.config.cjs"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "sheetmerge-core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
