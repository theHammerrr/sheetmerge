# Allow removing join mapping connections

In the join mapping modal, add a way to remove an existing connection between headers (e.g., click a linked header again to detach or add a delete control).
