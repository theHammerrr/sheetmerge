# Join mapping links from wrong origin

When mapping headers, connecting header A to B then B to C draws a line from A to C instead of from B to C. Fix link drawing so new connections originate from the selected header, not the group's first link.
