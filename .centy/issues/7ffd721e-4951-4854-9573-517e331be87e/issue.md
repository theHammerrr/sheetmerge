# Create web app (React + TS + Vite) for local merge

Build a web app that accepts multiple file uploads, lets users configure merge options, runs merge locally (browser mode), downloads merged file. Acceptance: drag & drop, config panel, download button, merge report, graceful error handling, tests. Note: if browser XLSX is too heavy, limit file size or restrict local mode to CSV initially.
