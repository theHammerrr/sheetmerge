# Fix web file picker replacing previous files

When adding files one-by-one in the web UI, the new selection replaces the existing list instead of appending. Ensure manual file picks append to current files (no duplicates).
