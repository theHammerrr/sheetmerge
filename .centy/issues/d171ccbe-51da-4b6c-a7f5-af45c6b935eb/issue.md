# Web structure: add layered boundaries in merge feature

Introduce explicit ui/application/infra layering in merge feature and place orchestration logic in application layer while UI hooks stay in ui layer.
