# Key Map Modal: Add Preview Button

Add a Preview button in the key map modal. On click, show a small table with the first 3 rows of the merged result so users can see expected output before confirming.
