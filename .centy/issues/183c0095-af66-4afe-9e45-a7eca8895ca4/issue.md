# Web structure: decompose use-merge-state responsibilities

Split use-merge-state responsibilities into focused hooks/services for join modal state and merge execution flow; keep hook thin and compositional.
