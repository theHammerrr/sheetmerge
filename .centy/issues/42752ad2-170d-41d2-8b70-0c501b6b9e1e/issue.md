# Web structure: introduce feature slices for merge and join mapping

Create feature-scoped entry points under apps/web/src/features and route App imports through feature modules instead of direct low-level modules.
