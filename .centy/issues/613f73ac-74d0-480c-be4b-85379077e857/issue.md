# Join mode column mapping modal

In join mode, after clicking Merge, show a modal with a row per input file and columns listing headers for that file. Let users map/connect the columns used for join keys across files before running merge. Use mappings to build join keys and proceed.
