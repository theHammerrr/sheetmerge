# Web styles: co-locate join mapping modal CSS with modal components

Move join mapping modal/grid/header/actions styles into CSS files next to each modal component and keep only global primitives in base styles.
