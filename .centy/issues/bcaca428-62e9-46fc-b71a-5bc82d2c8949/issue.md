# Parity: web export/import merge spec with joinKeyMaps

Add web-side functions to export current merge state to MergeSpec and import MergeSpec back into web state, including joinKeyMaps <-> join groups mapping.
